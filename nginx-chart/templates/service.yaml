apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.appName }}
  ports:
  - name: HTTP
    protocol: TCP
    port: 8080
    targetPort: 80